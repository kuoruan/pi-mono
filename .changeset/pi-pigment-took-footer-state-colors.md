---
"pi-pigment": patch
---

Took footers carry the call's state color: collapsed grep/find/ls tails render green (a tail footer only exists on success); error frames follow the failure kind (plain exits and non-shell tools error, timeouts/signals/aborts/terminations warn). Unmeasured rows show no footer; bash/powershell native timing stays muted.
