---
"pi-pigment": patch
---

settled output frames skip rebuilding the discarded placeholder (3.7x per frame), diff renders compute the split verdict once, and the shell path reuses its inert command.
